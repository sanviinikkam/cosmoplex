# button-coverage
All line numbers and data claims re-derived. Verdicts:

---

## SURVIVORS (4 of 6 claims, most severe first)

### 1. F1 — CONFIRMED · SHOULD-FIX (becomes BLOCKER the moment any 5-option question exists in the DB)
`C:/Users/rayaa/Desktop/AI101/cosmoplex/backend/api/whatsapp_routes.py:53` (`NUM_EMOJI` = 4 entries) crashed by `:1460` and `:1462` (`NUM_EMOJI[i] for i in range(len(opts))`).

Re-derived, not taken on trust:
- `len(NUM_EMOJI) == 4`; calling the real `_send_quiz_question` with 5 options raises `IndexError: list index out of range` **before any `_post`** (`sent count 0`); the 4-option control sends fine.
- The 5-option producer is live: `admin_routes.py:1418` `_OPT_RE` accepts `a-e`, and running the real `_parse_mcq` on `1. Which?\na) …\ne) five ✓` returns `options` of length 5, `correct_index: 4`. That feeds `bulk_quizzes` (`admin_routes.py:1671,1680-1686`) → `_clean_quiz` (`:1327` only enforces `>= 2`) → rows inserted verbatim. `create_quiz`/`update_quiz` (`:1052,1066`) validate option count not at all.
- Unrecoverable, as claimed: `_start_quiz` (`:1543-1550`) sets `stage="quiz"`, stores the set in `quiz_current`, **commits**, then sends. `_handle_message` (`:1668`) has no try/except, so the exception dies in the FastAPI background task. Every later message hits `stage in ("quiz","practice")` (`:1998`) and the type-instead-of-tap nudge (`:2044`) re-renders the same stored set → crashes again.

Repro: admin pastes/uploads a doc with an `e)` option → learner watches the lesson → taps **📝 Start quiz** → **no message ever arrives**, and nothing they send afterwards gets any reply. Only typing `restart` escapes, which nulls `language`/`stage` and wipes their place.

Where I refute the sweep: it billed this as certain-today. Every shipped question is 4-option (`whatsapp_content.QUIZ` counted: `{('en',4):5, ('hi',4):5, ('mr',4):5, ('te',4):5, ('ta',4):5, ('kn',4):5}`), and the manual admin editor hard-codes exactly four inputs (`frontend/app/admin/page.tsx:1705` `{[0,1,2,3].map(...)}`). So it is armed, not yet firing — unless the prod DB already holds an `e)` import, which I cannot see from here.

Fix: extend `NUM_EMOJI` to 10 (WhatsApp's row cap) or index it as `NUM_EMOJI[i] if i < len(NUM_EMOJI) else f"{i+1}."`, and cap options at `bulk_quizzes`/`QuizBody`.

---

### 2. F4 (partial) — CONFIRMED · SHOULD-FIX: `next_lesson` / `ask_doubt` are matched before any stage check, so a stale tap discards the current lesson's quiz + assignment
`whatsapp_routes.py:1972` (`next_lesson`) and `:1982` (`ask_doubt`) sit above the `stage in ("quiz","practice")` gate at `:1998` and the `stage == "assignment"` collector at `:2050`.

Repro A: finish lesson 1 → between-lessons card appears → tap **▶️ Next lesson** → lesson 2 → tap **📝 Start quiz** (stage `quiz`) → scroll up one screen to the *old* between-lessons card and tap **▶️ Next lesson** again → `_advance_lesson` (`:1352-1364`) sets `lesson_index += 1`, calls `_reset_quiz_state` (clears `quiz_index`, `quiz_correct`, `quiz_current`, `quiz_seen`), forces `stage="lesson"` and delivers lesson 3. Lesson 2's quiz gate never passed and its assignment never appeared. Symptom: the learner is silently one lesson ahead with an ungraded lesson behind them, and their quiz pool for that lesson is wiped.

Repro B: pass the quiz (stage `assignment`, prompt on screen) → scroll up, tap **I have a doubt** → `stage="clarify"` and the only button offered afterwards is `next_lesson`, so the assignment is abandoned even though `assignment_draft` is still stored.

Fix: guard both branches with `if session.stage not in ("quiz","practice","assignment")` (or ignore `next_lesson` unless stage is `between_lessons`/`clarify`/`done`).

I partially refute F4: the `confirm_number` (`:1937`), `howto_skip` (`:1946`) and `start_lesson` (`:1966`) instances are real code paths with the same shape, but the damage is bounded (stage bounced to `howto`/`lesson`, `lesson_index` intact, no quiz pool wiped) — same one-line fix, not separate findings.

---

### 3. F2 — CONFIRMED · SHOULD-FIX: a stale quiz row is eaten by the assignment collector and reported as the learner's answer
`ans_*` is matched only at `whatsapp_routes.py:2001`, inside the `:1998` stage gate. Verified `_extract` (`:865-869`) returns the **row title** as `text`, and `send_list`'s tuple order is `(id, title, description)` (`:259-274`) — so for a quiz row the title is the bare emoji (`:1462`) and the option text lives in the description.

Repro: pass the quiz → stage `assignment` → scroll up and tap any row on the quiz list you already answered → falls through to `:2050`, then `:2092-2099`: `"3️⃣"` is appended to `session.assignment_draft` and the learner is shown *"Answer added"* with a Submit button. Their graded submission now contains a stray emoji. At `lesson`/`done`/`quiz_failed` the same tap reaches `_teacher_answer`, i.e. the Teacher is asked to answer `"3️⃣"`.

Fix: at the top of the assignment branch, drop any message whose `reply_id` is not `submit_assignment`/`skip_assignment` (re-show the assignment instead of appending).

---

### 4. F3 — CONFIRMED · SHOULD-FIX: at `ask_name` and `ask_goal`, a stale button tap is stored as the learner's name/goal
`whatsapp_routes.py:1871-1877` — `candidate = (text or "").strip()` … `session.name = candidate[:40]`, with no `reply_id is None` check, and positioned above every `reply_id` branch (`:1927-1996`). `:1912` has the identical hole for the goal: `GOAL_MAP.get(reply_id) if reply_id in GOAL_MAP else (text or "").strip()`. The contrast the sweep draws is real: `ask_profile` at `:1889-1892` correctly guards with `elif text and reply_id is None`.

Repro: a learner mid-course types `restart` (`:1719-1728`) → language picker → taps a language (stage `new` ∈ `SIGNUP_STAGES`, verified `whatsapp_drip.py:55`) → stage `ask_name` → scrolls up and taps the still-live **▶️ Next lesson** button → `session.name == "▶️ Next lesson"`. It is then used in every greeting and printed on the PDF (`_deliver_certificate`, `:1290` takes the name from `session.name`).

I correct one detail: the button label is `"▶️ Next lesson"`, not `"🚀 Start next lesson"` (re-derived via `tr('en','start_next_btn')`).

Fix: `if session.stage == "ask_name" and reply_id is None:` (same for `ask_goal`'s free-text fallback).

---

## REFUTED (2 claims)

- **F5 (dead `menu` id + unused `ONBOARD` keys)** — factually accurate (grep confirms `"menu"` appears only at `:1806`, and `start_prompt`/`start_btn`/`ready_prompt`/`ready_btn` have 0 references in `whatsapp_routes.py`), but it is housekeeping, not a defect: the typed-keyword half of the condition is live, and no learner can reach the dead half. Reported as a finding it costs the owner attention for zero learner impact.
- **F6 (`lang_*` at `onboarded`/`howto` skips signup)** — the code reads exactly as claimed (`:1762` gates on `SIGNUP_STAGES`, which excludes `onboarded`/`howto`; `:1785` checks only `("new","welcome")`), but the stated harm is overstated to the point of being wrong: "signup" here is a two-tap number confirmation that stores no data — the only real loss is the optional, explicitly-skippable how-it-works walkthrough (`howto_skip` exists at `:1946`), and the learner lands correctly on lesson 1 with their name/status/goal intact. Nothing is stuck and no progress is lost. NITPICK at most; not worth a fix slot.

Also refuted within surviving claims: F1's "certain today" framing (all shipped content is 4-option), three of F4's five instances (bounded, not progress-losing), and F3's quoted button label.

---

# stage-machine
Verified every claim against the source. Line numbers in the sweep are accurate for this working tree (`whatsapp_routes.py`, 2154 lines, HEAD `66dba74`). Survivors below, most severe first.

---

## 1. CONFIRMED — BLOCKER — global `next_lesson` / `quiz` / `retake` / `practice_quiz` handlers are not stage-guarded
`C:/Users/rayaa/Desktop/AI101/cosmoplex/backend/api/whatsapp_routes.py:1972` (also 1990, 1994)

Traced the full ordering of `_handle_message`: `ask_name` (1863), `ask_profile` (1887) and `ask_goal` (1911) branches sit **above** 1972, so those three stages are protected. Every other stage branch — `quiz`/`practice` (1998), `assignment` (2050), `between_lessons`/`clarify` (2111), `lesson` (2124), `done` (2141) — sits **below** it, so a stale tap is consumed at 1972/1991/1994 first. `_advance_lesson` (1352) checks nothing but `len(lessons)`.

The repo itself establishes that stale taps are a real user behaviour: the comment at 1757 reads "WhatsApp lets a learner scroll up and tap an OLD list", which is why the `lang_*` path got its `SIGNUP_STAGES` guard. The same is true of quick-reply buttons.

Reproduction A (progress loss): finish lesson 1 → between-lessons screen → tap **▶️ Start next lesson** → lesson 2 → tap **▶️ Start quiz** → answer 2 of 5 questions → scroll up one message and tap the still-live **▶️ Start next lesson** → `_advance_lesson` fires, `_reset_quiz_state`, `stage = "lesson"`, lesson 3 is delivered. The half-finished quiz is gone with no warning.

Reproduction B (credential integrity): from the between-lessons screen, tap **▶️ Start next lesson** repeatedly (one tap per lesson). No quiz, no assignment, `_is_last_in_module` never consulted, so the compulsory module-final assignment is bypassed; the last tap sets `stage = "done"` and `_advance_lesson:1368` calls `_deliver_certificate`, whose own docstring at 1281 asserts `done` is "reached only after passing every lesson's quiz". That invariant is false.

Reproduction C: at `stage == "assignment"` (quiz already passed), tap a scrolled-up **▶️ Start quiz** → 1991 `_start_quiz` resets `quiz_index`/`quiz_correct` and drags them back into a quiz they passed.

Symptom: learners silently lose quiz progress mid-attempt, and a certificate can be minted without a single quiz or assignment.
Fix: move lines 1972–1995 below the stage branches, or gate each on the stage that legitimately offers that button (`next_lesson` → `{between_lessons, clarify}`, `quiz`/`retake` → `{lesson, quiz_failed}`).

## 2. CONFIRMED — BLOCKER — `_resume_stage`'s else-branch is a blanket backwards transition, fired by ordinary typed text
`whatsapp_routes.py:1515-1519`, callers at `1769` and `1792`

Only `quiz`, `practice` and `assignment` resume in place. `between_lessons`, `clarify`, `done`, `onboarded`, `quiz_failed` and `howto` all hit the `else` and are **forced** to `stage = "lesson"` with the current lesson video re-sent. The typed-language guard at 1785 only special-cases `("new", "welcome")`, and the `lang_*` guard at 1762 only covers `SIGNUP_STAGES` = `{new, welcome, ask_name, ask_profile, ask_goal}` (`whatsapp_drip.py:55`) — neither covers the six stages above.

Trigger is trivially cheap: `_detect_language` (1486) is a bare substring match over `LANG_KEYWORDS` with a 5-word ceiling, and it runs on voice-note transcriptions too (`_handle_audio:382`).

Reproduction: finish lesson 3's quiz + assignment → between-lessons screen → type "I prefer English please" (4 words, contains "english") → `stage = "lesson"`, lesson 3's video re-sent, and the live screen now offers only *Start quiz / Quiz language / Course language*. Same message at `stage == "done"` ("thanks, the english was great" — 5 words, passes the ceiling) pulls a graduate out of the terminal state back to `lesson`.

Symptom: a learner who has completed lesson N is put back at the start of lesson N and, going by the fresh screen, must re-sit a quiz and assignment they already passed; a graduate is pulled out of `done` and re-enrolled in the final lesson.
Fix: in the `else`, resume the actual stage (re-send the between-lessons/clarify/retake/done buttons for those stages) instead of unconditionally assigning `"lesson"`.

*Two sub-claims in the sweep's #2 do not hold and I have dropped them: (a) "the Start next lesson button is gone from the live screen" — the old button message is still tappable (that is precisely finding 1), so this is a screen-state complaint, not a hard block; (b) the `howto` case is milder than claimed — the learner lands on lesson 1, which is exactly where the walkthrough's own "Let's start" button leads, so they skip a tutorial rather than getting stuck. The `onboarded` case is real and is finding 4's neighbour: signup/number-confirmation is skipped entirely.*

## 3. CONFIRMED (mechanism), REPRO CORRECTED — SHOULD-FIX — `restart` keeps `lesson_index`
`whatsapp_routes.py:1719-1728`

The block resets `language`, `stage`, `opt_out`, `quiz_index`, `quiz_correct` — and nothing else. Compare `cset_restart` at 1846-1849, which does `lesson_index = 0` + `_reset_quiz_state` + `assignment_draft = None`. The comment at 1720 claims "fresh state".

Reproduction: reach lesson 6 → type "start over" → re-pick language, re-type name, re-answer profile + goal, tap Sign up, confirm number, finish the walkthrough → the final **Let's start 🚀** button is `start_lesson` (1963), which sends `session.lesson_index or 0` = **lesson 6**.

Symptom: "start over" replays the whole onboarding and then drops the learner back where they already were — the one thing they asked to undo.
Fix: add `session.lesson_index = 0`, `_reset_quiz_state(session)`, `session.assignment_draft = None` to the restart block.

*Refuted sub-claim: "a stale `assignment_draft` survives, so the next Submit grades text written before the restart." Line 2016 sets `session.assignment_draft = None` on entry to `stage = "assignment"` after a quiz pass, so the stale draft is cleared before any Submit is reachable in the normal flow.*

## 4. CONFIRMED — SHOULD-FIX — `stage == "onboarded"` has no handler, so the Sign-up CTA is never re-offered
`whatsapp_routes.py:1918` assigns it; no branch anywhere tests it (verified by grepping every `session.stage ==` / `session.stage in` site).

Reproduction: answer the goal question → `stage = "onboarded"`, saved + free_offer + a single **Sign up** button (1926-1927). Reply with text instead of tapping — "yes", "ok ready", "how much is it" — or a voice note. Nothing matches; execution reaches the final fall-through at 2153 and they get a free-form Teacher answer with **no button and no next step**. The only thing that ever re-offers the CTA is the 2-hour `start_lesson` template (`whatsapp_drip.py:68`), which is plain text.

Symptom: at the single conversion step of the funnel, an enthusiastic text reply gets chat instead of the signup button. Every other conversational stage (`lesson` 2124, `clarify` 2111, `quiz_failed`→retake) re-offers its buttons.
Fix: add a branch before 2153 — at `stage == "onboarded"`, answer via `_teacher_answer` then re-send the `signup` button.

## 5. CONFIRMED — SHOULD-FIX — `howto` and `practice` have no drip rule at all
`whatsapp_drip.py:55` and `67-77`; `_stage_nudge` falls through to `return None` at 199.

Re-derived the stage sets from the source rather than trusting the sweep: 15 stages are assigned (`new, welcome, ask_name, ask_profile, ask_goal, onboarded, howto, lesson, quiz, practice, quiz_failed, assignment, between_lessons, clarify, done` — `quiz`/`practice` come from the ternary at 1545). `SIGNUP_STAGES` covers 5, `NUDGE_RULES` covers `{onboarded, lesson, between_lessons, clarify, quiz, quiz_failed, assignment, done}` = 8. Total 13. **`howto` and `practice` match no rule.**

Reproduction (`howto`): tap Sign up → tap Confirm number → read walkthrough step 1 → put the phone down. `stage = "howto"` forever, no nudge ever fires, and they are one tap from lesson 1.
Reproduction (`practice`): tap **🎯 Practice quiz** from the between-lessons or `done` screen, answer 2 questions, stop. Invisible to the drip, and their `between_lessons`/`done` cohort membership is gone too, so the nudge they *would* have had is also lost.

Symptom: the segment that gets furthest through signup without starting a lesson is the one segment never re-engaged.
Fix: add `("start_lesson", {"onboarded", "howto"}, 2)` and `{"quiz", "quiz_failed", "practice"}` to the `finish_quiz` rule.

## 6. CONFIRMED — SHOULD-FIX — a failing certificate delivery makes `stage == "done"` swallow every message forever
`whatsapp_routes.py:1336-1347` and `2141-2146`

`_deliver_certificate` returns `True` as soon as it has announced (1347), but only sets `session.certificate_pdf` when `send_document` returns `True` (1343-1344). `send_document` returns `False` on any non-2xx (208-211). The `done` branch at 2145 does `if await _deliver_certificate(...): return`.

Reproduction: finish the course while `BACKEND_URL` points somewhere Meta cannot fetch (or after a redeploy wiped the ephemeral `certificates/` dir between generation and fetch) → send any message → a fresh PDF is generated under a new UUID, `cert_ready` is sent again, the document send 4xxs, `return True`, `return`. Repeat for every message the learner ever sends.

Symptom: a graduate asking a question gets "your certificate is ready" plus a link and never an answer — every single time; `certificates/` grows one PDF per inbound message.
Fix: have `_deliver_certificate` return `True` only on the turn it *first* announces (track it separately from `certificate_pdf`), or fall through to the Teacher when delivery failed.

## 7. CONFIRMED (mechanism), REPRO REFUTED — SHOULD-FIX — `_resume_stage` does not clamp `lesson_index`
`whatsapp_routes.py:1519` vs. `1843-1845`

The code asymmetry is real: `cset_resume_XX` deliberately clamps with `min(session.lesson_index or 0, max(0, len(lessons) - 1))`; `_resume_stage` passes `session.lesson_index or 0` raw into `_send_lesson`, and `_lesson_at` (1082-1084) returns `None` out of range → `tr(lang, "no_more")` with the index left permanently out of range. The downstream consequence the sweep describes also checks out: `_current_video_id` → `None` → `_all_quiz(None)` falls back to the 5 built-in questions (1121-1122), the built-in assignment (1160), then `_send_between_choice` (1374) computes `cur + 1 < len(lessons)` = false → `stage = "done"` + certificate off the placeholder quiz.

**The sweep's reproduction is impossible and I am replacing it.** `_variant_public_id` (1044-1048) is `by_lang[lang] or by_lang["en"] or video.cloudinary_public_id`, so the inclusion test for any non-English language is a strict *superset* of the test for English. A `kn` playlist can therefore never be shorter than the `en` one, and "reach lesson 9 in `en`, type kannada" cannot dead-end. The reachable direction is the reverse: a learner in a language that has videos English lacks — a video whose only `VideoLanguageVariant` is e.g. `hi` and whose base `cloudinary_public_id` is `None`, which the admin API permits (`admin_routes.py:877` makes it `Optional`, and `1770` only backfills it from an `en` mapping). Reproduction: reach the last lesson in `hi` on a course with any hi-only video, then type "english" → the `en` list is shorter → out-of-range dead end and, via **Start quiz**, a certificate off the built-in quiz.

Fix: apply the same `min(...)` clamp inside `_resume_stage` before calling `_send_lesson`.

## 8. CONFIRMED — NITPICK — the Skip button is dropped when an optional assignment is re-rendered
`whatsapp_routes.py:1514` (and the same omission at `2106`)

`_send_assignment(frm, lang, assignment)` takes the `skippable=False` default. `_rerender_quiz_step` (1532-1534) and the quiz-pass path (2032-2034) both compute `not _is_last_in_module(...)`; these two calls do not.

Reproduction: at `stage == "assignment"` on a non-last microlesson, tap a scrolled-up language row (hits 1514), or tap a stale `ans_N` quiz answer (falls to 2106). The re-rendered assignment shows only **Submit**.

Symptom: an assignment the product treats as optional becomes, on screen, compulsory — and `stage` stays `assignment`, whose drip nudge keeps telling them to submit it.
Fix: compute `skippable = not _is_last_in_module(lessons, session.lesson_index or 0)` at both 1514 and 2106, as the other two call sites already do.

## 9. CONFIRMED — NITPICK — `ask_doubt` at `stage == "done"` walks a graduate backwards and switches their drip cohort
`whatsapp_routes.py:1982-1987`, `1391-1397`

At `done` the menu is *Practice quiz / Invite & earn / I have a doubt*. **I have a doubt** sets `stage = "clarify"` and offers only `next_lesson` (1986). Tapping it → `_advance_lesson` → no next lesson → `stage = "done"` and the course-complete text is announced again. Meanwhile `clarify` matches the `next_lesson` nudge (`whatsapp_drip.py:73`), so a graduate is nudged to "start your next lesson" when none exists.

Symptom: cosmetic for the doubt itself (the Teacher does answer at 2113), but the graduate is told to continue a finished course, by button and by nudge.
Fix: at `stage == "done"`, keep `stage = "done"` on `ask_doubt` and offer the done-menu buttons instead of `next_lesson`.

## 10. CONFIRMED but MECHANISM CORRECTED — NITPICK — `welcome` is committed before three sends and `ask_profile` only after
`whatsapp_routes.py:1019-1027`

The ordering fragility is real: `welcome` is a stage nothing branches on, and `ask_profile` is assigned only after `send_text`, `_intro_video_for`, `send_video` and `_send_profile_question` all return.

**The sweep's stated triggers are wrong, which is why this is a nitpick and not a blocker.** `send_text`/`send_buttons`/`send_video` all route through `_post` (215-231) and `_download`/`_upload_media`, every one of which catches `httpx.HTTPError` and returns `None`/falls back to a text link — a Cloudinary or Meta failure cannot strand anyone. Only an unswallowed exception can: a DB error inside `_intro_video_for`'s `select` (1013-1016), a `_log_wa_message` failure, or a non-`HTTPError` such as a `JSONDecodeError` from `r.json()` at 310. `_handle_message` runs as a `background_tasks` task (850), so such an exception is logged and never retried. Two escapes exist even then (a typed language name → 1785's `("new","welcome")` → `ask_name`; a `lang_*` tap → 1762, since `welcome ∈ SIGNUP_STAGES`), and the signup drip does cover `welcome`.

Symptom (narrow): after a DB blip the learner is never asked the profile question again and gets a Teacher reply to everything.
Fix: assign `session.stage = "ask_profile"` and commit before the sends.

---

**Refutation summary.** No claim was wholly fabricated, but I rejected or rewrote five specific assertions: (1) claim 3's reproduction — an `en`→`kn` switch cannot shrink the playlist, because `_variant_public_id` falls back to the `en` variant and then the base id, making every non-English playlist a superset of English's; I supplied the reachable direction instead. (2) Claim 6's stale-`assignment_draft` half — line 2016 clears the draft on entry to the assignment stage, so it can never be graded post-restart. (3) Claim 5's triggers — `send_video`/`send_text`/`send_document` all swallow HTTP failures and fall back, so the named Cloudinary/Meta failures cannot strand anyone at `welcome`; downgraded to NITPICK on the surviving DB-error path. (4) Claim 9's "a learner who lost the PDF has no route to it" as a defect — `send_document:210` already falls back to sending the fetchable link as text; the missing *keyword* to re-request it is a feature gap, not a bug, so I folded it away and kept only the message-swallowing loop. (5) Claim 2's `howto` sub-case and its "the button is gone from the live screen" framing — the `howto` victim lands on lesson 1, which is where the walkthrough leads anyway, and stale button messages remain tappable by claim 1's own premise.

---

# quiz-flow
All five claims traced against real code. Nothing was already fixed; the code matches the sweep. Survivors below.

---

**1. VERDICT: CONFIRMED — SEVERITY: BLOCKER**
`backend/api/whatsapp_routes.py:1972` (`next_lesson`), `:1990` (`quiz`/`retake`), `:1993` (`practice_quiz`) — matched on `reply_id` alone, all three sit **above** the stage blocks at `:1998` (quiz/practice) and `:2049` (assignment). I read `_handle_message` from `:1668` to `:1972` line by line: there is no earlier stage guard, no "stale tap" check, and no early return that shields these three ids (the only stale-tap defence in the file is the `SIGNUP_STAGES` check on `lang_*` at `:1755`).

Repro (simplest form, no practice quiz needed): finish lesson 1's quiz → between-lessons card appears with *Start next lesson*. Tap it → lesson 2 (stage `lesson`). Scroll up one message and tap that **same** card's *Start next lesson* again → `:1972` → `_advance_lesson` (`:1352`) → `lesson_index += 1`, `_reset_quiz_state`, lesson 3 delivered. Repeat taps on the one stale card to walk the entire course; the last tap sets `session.stage = "done"` and calls `_deliver_certificate` (`:1278`), whose docstring claims `done` is "reached only after passing every lesson's quiz."
Variant B (mid-quiz): during lesson N's graded quiz, tap the stale *Start next lesson* → quiz progress discarded, lesson N+1 sent, assignment never seen.
Variant C (silent loss): in stage `assignment` with a long typed draft, tap the stale *Start quiz* → `:1990` restarts the quiz; on re-pass `:2026` sets `assignment_draft = None`, wiping the typed answer with no warning.

Learner-visible symptom: quizzes and assignments are optional in practice, and a learner who has answered nothing can receive a completion certificate; a diligent learner can lose a half-finished quiz or a typed assignment to one mis-tap.
Fix: gate all three ids on stage — ignore/`re-render current step` for `next_lesson` unless `session.stage in ("between_lessons","clarify","done")`, and for `quiz`/`practice_quiz` unless stage is `lesson`/`quiz_failed`/`between_lessons`.

---

**2. VERDICT: CONFIRMED (code) — SEVERITY: SHOULD-FIX (becomes a BLOCKER for any lesson whose DB bank has <3 questions — verify in prod)**
`whatsapp_routes.py:1129-1132` + `:2024`. `_select_quiz` picks `random.sample(pool, min(QUIZ_PER_ATTEMPT, len(pool)))`, so a bank of 1-2 questions yields a 1-2 question attempt; grading compares `score >= QUIZ_PASS` with `QUIZ_PASS = 3` (`whatsapp_content.py:120`), never `len(items)`. Max score 2 → `stage = "quiz_failed"` → only button is *Retake* → same short bank → fail forever.
I re-derived the two enabling facts: (a) the built-in fallback bank is safe — `whatsapp_content.QUIZ` has exactly 5 questions, every one with 4 options in all 6 languages, so no learner on the fallback is affected; (b) a 2-question import survives the importer — running the real `_parse_mcq` logic (`admin_routes.py:1428`) on a 2-option/2-question doc returns the item, `_clean_quiz` (`:1319`) only rejects `len(en_opts) < 2`, and `bulk_quizzes` (`:1685`) only rejects an empty list. `POST /videos/{id}/quizzes` (`:1046`) has no bank-size check either.
Repro: import a 2-question MCQ doc for a lesson → learner taps *Start quiz* → answers both correctly → "your score: 2/5. You need 3/5" → *Retake* → identical loop, assignment and all later lessons unreachable.
Fix: `pass_mark = min(QUIZ_PASS, len(items))` at `:2024` (and reject banks smaller than `QUIZ_PASS` at import).
Sub-note (cosmetic, same trigger): `quiz_progress`/`score_pass`/`score_fail` hardcode "/5" in all six languages (`whatsapp_content.py:162,166,167` and the per-language rows) — a 3-question attempt says "Question 1/5" and reports "2/5".

---

**3. VERDICT: CONFIRMED (code) — SEVERITY: SHOULD-FIX (BLOCKER the moment one 5-option question exists in the bank — check the DB before shipping)**
`whatsapp_routes.py:53` `NUM_EMOJI` has 4 entries; `_send_quiz_question` indexes it unbounded at `:1460` and `:1462`. Five options → `IndexError` (I reproduced the exact indexing failure, and confirmed `_parse_mcq`'s `_OPT_RE = r"^\(?([a-eA-E])[.)]\s*(.+)$"` (`admin_routes.py:1418`) parses an `e)` option through to 5 stored options; `_clean_quiz` and the unvalidated `QuizBody.options` dict at `:1031` add no cap).
The silence is real: `_handle_message` is queued as a background task at `:850` and its body (`:1668` onward) has **no** try/except; the webhook's own `try` (`:818-853`) has already returned `{"status":"ok"}`. So the learner receives nothing at all.
Repro: for a lesson with a 5-option question at position 1, tap *Start quiz* → `_start_quiz` (`:1544`) commits `stage="quiz"`, `quiz_index=0`, then the send raises → no question arrives. Every escape re-enters the crash: any typed message → `:2045` resend → raises; a quiz-language switch → `_rerender_quiz_step:1527` → raises; a course-language pick is the only way out, and only via *Restart from beginning*.
Fix: build the row labels with `NUM_EMOJI[i] if i < len(NUM_EMOJI) else f"{i+1}."` and cap options at 4 in `_clean_quiz`/`QuizBody`.

---

**4. VERDICT: CONFIRMED — SEVERITY: SHOULD-FIX**
`whatsapp_routes.py:2002-2014`. `qidx = session.quiz_index or 0` comes from the session; `chosen = int(reply_id.split("_",1)[1])`; `base = items[qidx]` and `_shuffle_options(base, frm, qidx)` re-derive the ordering for the **current** index. The `ans_N` id carries no question identity, so any older list row grades against whatever question is current.
Repro: on Question 4, scroll up and tap Question 2's *2️⃣* row (or double-tap the row you just answered when the reply is slow) → option 2 is graded against Question 4, `quiz_index` advances to 5, Question 5 is delivered. On a mismatch the learner is told "the correct answer is <option of a question they never looked at>".
Learner-visible symptom: questions are silently consumed or mis-scored, the attempt can end early with an unexplained low score, then the *Retake* loop.
Fix: encode the index in the id (`ans_{qidx}_{choice}`) and ignore a reply whose `qidx` ≠ `session.quiz_index`.

---

**5. VERDICT: CONFIRMED — SEVERITY: NITPICK**
`whatsapp_routes.py:2092-2099` with `_extract` (`:857`) and the row shape at `:1462`. Quiz list rows are `(f"ans_{i}", NUM_EMOJI[i], opts[i])` and `send_list` (`:273`) puts the 2nd element in `title`, so a list reply returns `text = "2️⃣"`. In stage `assignment`, `ans_*` matches neither `skip_assignment` nor `submit_assignment` and falls to `if text and text.strip():` → `assignment_draft += "\n2️⃣"` → "Answer added" + Submit.
Repro: pass the quiz, land on the assignment, scroll up, tap any old quiz option → confirmation that doesn't match what they did, and a graded submission containing junk lines (lowering the LLM score against `ASSIGN_PASS`). In stage `quiz_failed` the same tap falls through to the Teacher at `:2153`, which answers "2️⃣" as a question.
Fix: at the top of the assignment block, drop any `reply_id` starting with `ans_` (re-show the assignment instead of appending).

---

**Refuted / downgraded (1 sub-claim refuted, 2 severity corrections):**
- Refuted as filable: the "latent crash" in claim 4, `base = items[qidx] if qidx < len(items) else items[-1]` → `IndexError` on empty `items`. I traced every writer of `quiz_current`: it is only ever set by `_select_quiz:1133` (non-empty, since `_all_quiz` always falls back to the 5-question built-in bank) or cleared by `_reset_quiz_state:1247`, and every caller of `_reset_quiz_state` (`:1360`, `:1739`, `:1856`) sets `stage` to `lesson` in the same commit — so stage `quiz` with empty `items` is not reachable. Not a defect today.
- Claims 2 and 3 are correct as code, but their triggers are **unverified data**, not live facts: there is no local DB in this repo to query, and I re-derived that the built-in fallback bank cannot trigger either one (5 questions × 4 options, all languages). The sweep's "CONFIRMED, current code / learner goes permanently silent" framing overstates them until someone runs `SELECT video_id, count(*) FROM quiz_questions GROUP BY video_id HAVING count(*) < 3;` and a `jsonb_array_length(options->'en') > 4` check. Both are cheap fixes worth taking regardless.
- Claim 1's Repro A (via *Practice quiz*) is real but needlessly long — the same bypass needs only repeated taps on one stale *Start next lesson* card, which is the repro I filed.

---

# assignment-flow
SURVIVORS (13), most severe first. All paths traced in `C:\Users\rayaa\Desktop\AI101\cosmoplex\backend\api\whatsapp_routes.py` unless noted.

---

**1. VERDICT: CONFIRMED — BLOCKER — `whatsapp_routes.py:1972` (`next_lesson`) and `1993-1995` (`practice_quiz`), both above the `stage == "assignment"` block at `2050`**

Traced the whole span 1668→2050: nothing between them returns for `stage == "assignment"` (the only stage-aware early exits are `unsubscribe`, `ask_name/ask_profile/ask_goal`, `howto`, and the `_detect_language` block at 1786 which deliberately excludes `"assignment"`). So both handlers are reached with `stage == "assignment"` intact.
Repro A: sit on a module's last (compulsory) assignment → scroll up to the previous lesson's "Start next lesson" button → tap. `_advance_lesson` (1352-1364) sets `lesson_index+1`, `stage="lesson"`. The compulsory assignment is never done and is now unreachable (`stage="assignment"` is entered only from a quiz pass on the *current* `lesson_index`, 2024-2026).
Repro B: tap a stale "Practice quiz" button → answer 5 questions → 2019-2023 calls `_send_between_choice`, `stage="between_lessons"`. Practice explicitly "doesn't gate progress" yet discharges a compulsory assignment.
On the **final** lesson both land on `stage="done"` (`_advance_lesson:1365-1368`, `_send_between_choice:1389-1396`) and call `_deliver_certificate`, whose only gate is `session.stage != "done"` (`1288`) — certificate issued with the final compulsory assignment never submitted. Same handler also fires at `stage="quiz_failed"`, advancing past a failed quiz.
Fix: guard both handlers — `if reply_id == "next_lesson" and session.stage not in ("assignment", "quiz", "practice", "quiz_failed")`, else re-render the current step.

**2. VERDICT: CONFIRMED — BLOCKER — `whatsapp_routes.py:525-528` + `2082-2089`**

`grade_answer`'s bare `except` returns `(0, "Sorry — I couldn't evaluate that just now…")`; the caller cannot distinguish it from a real grade. `0 < ASSIGN_PASS` (40) → fail branch, and `session.assignment_draft = None` at **2083 runs before** the pass/fail test, then `await db.commit()` at 2088 persists the deletion.
Repro: type a good multi-message answer, tap Submit while the Anthropic call fails (expired key, 429/529, network blip, non-JSON from Haiku). Learner sees `⏳ Checking your answer…` then `📝 <name>, you scored 0/100 — you need 40/100 to pass.` with the apology as the "feedback", and their whole answer is gone from the DB.
Correction to the sweep: the wrapper (`assign_fail`) **is** localized in all six languages (`whatsapp_content.py:187, 226, 265, 304, 343, 382`); only the interpolated `{f}` apology sentence is English-only. The finding stands regardless.
The `allow_ai_call()` branch at 2071-2076 deliberately preserves the draft for exactly this situation; the API-error path does the opposite.
Fix: have `grade_answer` return `None` on exception and treat it like the `ai_busy` branch (draft untouched, no score shown).

**3. VERDICT: CONFIRMED — SHOULD-FIX (high) — `whatsapp_routes.py:857-870` + webhook dispatch at `849-853`**

`_extract` handles only `text` and `interactive` (`button_reply`/`list_reply`), returning `(None, None)` for everything else; the loop only queues `_handle_message` when `reply_id or text` is truthy. `audio` is special-cased at 841. So `image`, `document`, `video`, `sticker`, `location` are dropped before any handler runs — no reply, no draft change, no transcript row.
Repro: on the assignment, photograph your handwritten answer (or attach a PDF) and send. Nothing happens, ever. Then tapping Submit gives "I don't have your answer yet" — reads as the bot losing their work. Onboarding copy at `950` ("type or send a voice note") never warns against attachments.
Unverified add-on from the sweep: `type: "button"` (template quick-reply) is likewise unhandled by `_extract`; whether any live nudge template carries quick-reply buttons is defined in Meta, not this repo, so I could not confirm that half.
Fix: in `_extract`, return a sentinel for unsupported media types and reply with a localized "please type or send a voice note instead".

**4. VERDICT: CONFIRMED — SHOULD-FIX — `whatsapp_routes.py:2064-2090`, dedupe at `64-73`**

Two Submit taps are two webhook POSTs with distinct message ids, so `_seen_before` does not dedupe. Each gets its own background task and its own `async_session_factory()` session (1677). Both read the same non-empty draft at 2065; `allow_ai_call()` (`core/spend_guard.py:23-35`) is a global daily counter, not a per-user lock, so both pass and both call Haiku at default temperature. Learner gets two `⏳ Checking…` messages and two independent scores that can straddle the 40 threshold — e.g. pass menu followed by "you scored 20/100, send an improved answer" with the stage already moved on. Double AI spend. `_seen_ids` is process-local, so with >1 uvicorn worker/replica a Meta re-delivery double-grades too.
Fix: set a per-session grading flag (or clear the draft into a local var under a row lock) before awaiting `grade_answer`.

**5. VERDICT: CONFIRMED — SHOULD-FIX — `whatsapp_routes.py:2083`, `2088-2089`**

Narrowed from the sweep's version. A genuine 35/100 also loses the entire draft (2083 is unconditional), and the fail message is a bare `send_text` — no Submit button and no re-shown question, unlike the quiz fail which offers Retake (2038-2041). A near-miss answer must be retyped from scratch on a phone rather than tweaked.
**Refuting part of the claim:** "the learner cannot improve what is no longer there" and "recovery only happens if they happen to type fresh text" are overstated — the fail copy tells them to send an improved answer, and any typed text hits the append branch at 2092-2100 which re-offers Submit. The real residual defects are the retyping-from-zero data loss and that a stale-Submit tap answers "I don't have your answer yet" (2066-2069).
Fix: keep the draft on a fail (or echo it back) and send the fail message with `_send_assignment`-style buttons.

**6. VERDICT: CONFIRMED — SHOULD-FIX — `whatsapp_routes.py:1514` and `2106`**

`_resume_stage` re-sends the assignment with `_send_assignment(frm, lang, assignment)` — `skippable` defaults to `False` (1467-1472) — while `_rerender_quiz_step:1531-1534` computes it correctly from `_is_last_in_module`. Line **2106** (the "anything else" fallback inside the assignment branch) has the same omission; the sweep missed this second instance.
Repro: on an *optional* assignment, tap a scrolled-up course-language list row (`lang_XX`, 1749-1762 → `_resume_stage`), or tap any unrecognized stale button → the assignment comes back with no "Skip for now" button, and stays that way for the rest of that lesson.
Fix: compute `skippable = not _is_last_in_module(...)` in both places, as `_rerender_quiz_step` already does.

**7. VERDICT: CONFIRMED — SHOULD-FIX — `whatsapp_routes.py:2053-2057`**

Skip clears the draft and drops into `_send_between_choice` → `between_lessons`, so forward progress works. But nothing records the skip and no command, button, or transition ever returns to a previous lesson's assignment — `stage="assignment"` is set only at 2025 for the *current* `lesson_index`. The button label promises the opposite: "Skip for now" / "अभी छोड़ें" / "ಸದ್ಯಕ್ಕೆ ಬಿಡಿ" (`SKIP_BTN`, 1256-1263).
Fix: either persist skipped lessons and add a "pending assignments" entry point, or relabel to "Skip" / "Not now — skip permanently".

**8. VERDICT: CONFIRMED — SHOULD-FIX — `db/models.py:351-392` + `whatsapp_routes.py:1288`**

Re-derived from the model: `WhatsAppSession` has exactly one assignment column, `assignment_draft`. No score, pass flag, skip flag, or submission row exists anywhere for WhatsApp assignments (the `/admin` learner-detail assignment view at `api/admin_routes.py:630-679` is web-learner-only). The certificate's sole gate is `stage != "done"`. So quiz results gate progression (you cannot reach `stage="assignment"` without `score >= QUIZ_PASS`) while assignment outcomes gate and record nothing.
Correction to the sweep's framing: progression past a *compulsory* assignment **is** gated inside the assignment branch (2058-2061 refuses skip on a last-in-module lesson) — what's missing is any durable record, plus the bypasses in finding 1. No admin-visible signal of who skipped or failed what.
Fix: add `assignment_score` / `assignment_skipped` (per lesson) and surface them in the admin learner view.

**9. VERDICT: CONFIRMED — SHOULD-FIX — `whatsapp_routes.py:1146-1157`**

`_assignment_for` takes `res.scalars().first()` ordered by `order_index, created_at`, so only prompt #1 per video is ever used. `POST /videos/{video_id}/assignments/bulk` (`api/admin_routes.py:1702-1706`) appends N extracted prompts. Prompts 2..N are content an admin uploaded and paid translation for that no learner can ever see.
Fix: pick by a per-session index or document in the admin UI that only the first prompt is served.

**10. VERDICT: CONFIRMED — NITPICK — `whatsapp_routes.py:2066-2069`**

The `len(draft) < 10` guard answers a genuinely short answer with `submit_empty` ("I don't have your answer yet") one message after the learner saw "Got it ✍️". Nine characters is easy to reach in Indic scripts.
Fix: distinguish empty (`not draft`) from too-short, with a "that looks very short — add a bit more" message for the latter.

**11. VERDICT: CONFIRMED — NITPICK — `whatsapp_routes.py:2097`**

`((old or "") + "\n" + new).strip()[:8000]` truncates the **head-kept** string, so once the draft reaches 8000 chars every further message is silently discarded while the learner is still told "Got it ✍️" (2099).
Fix: if the append would exceed the cap, say so instead of silently dropping.

**12. VERDICT: CONFIRMED — NITPICK — `whatsapp_routes.py:395`**

Voice notes themselves are fine (`_handle_audio:367-387` transcribes, logs an `audio` row, re-enters `_handle_message` with `already_logged=True`, lands on the append branch — verified). On transcription failure or budget exhaustion the draft is correctly untouched, but `VOICE_FAIL` is a bare `send_text`: no Submit button and no re-shown question, so mid-assignment the learner is left with no control on screen.
Fix: on `stage == "assignment"`, send `VOICE_FAIL` with the Submit button attached.

**13. VERDICT: CONFIRMED — NITPICK — `whatsapp_routes.py:1697`, `1714`, `1719`, `1806`**

A typed assignment message that is *exactly* `unsubscribe`, `refer`, `restart`, `menu`, `language`, `lang`, or a language name in `low` is executed as a global command instead of appended (exact match on `(text or "").strip().lower()`, so no false positives on longer text). Note `_detect_language` at 1786 already excludes `"assignment"` — this residue is only the fixed keyword lists. Cost is a detour, not data loss (the draft survives; `lang_XX` → `_resume_stage` returns them).
Fix: skip the keyword commands (except `unsubscribe`) when `session.stage == "assignment"`.

---

**Refuted / corrected — 1 claim dropped, 3 narrowed:**

- **Dropped: the admin-rubric `ASSIGN_PASS` interpolation note (8f).** Speculation, not grounded. `whatsapp_content.py:148` interpolates the placeholder only because the built-in rubric literally contains `{ASSIGN_PASS}`; `row.rubric` is admin/AI-authored text, and `ASSIGN_SYS` (`admin_routes.py:1261-1266`) never emits a pass mark or that placeholder. The defect only exists for a hypothetical rubric someone might one day write.
- **Narrowed (finding 2):** the sweep said the fail message is English "whatever their language" — the score/threshold copy is localized in all six languages; only the fallback feedback sentence is English.
- **Narrowed (finding 5):** "cannot improve what is no longer there" / "recovery only happens if they happen to type fresh text" — typing is exactly what the copy instructs and it does restore the Submit button; the surviving defect is the retype-from-zero loss and the misleading stale-Submit response.
- **Narrowed (finding 8):** "assignments gate nothing" — the compulsory gate does work inside the assignment branch; what is absent is any persisted record (and the bypasses in finding 1).
- The sweep's "verified clean" draft-leak analysis (8, Q1) checks out: every route into `stage="assignment"` passes through the clear at 2026, plus 2056, 2083, and 1859 — no stale draft can reach the next lesson's grader. Not a finding, correctly reported.
- Line-number drift: the sweep's certificate-gate cite (1277-1281) is actually `1288`; `next_lesson` is `1972` not `1966-1968`; `_advance_lesson` is `1352`. Substance unaffected.
- No "ALREADY FIXED TODAY" list was included in the brief I received, so I could not screen against it; nothing above was contradicted by a fix visible in the working tree.

---

# onboarding-switch
## SURVIVORS (9 of 9 filed findings partially or fully confirmed; 4 sub-claims refuted)

---

### 1. VERDICT CONFIRMED — SEVERITY **BLOCKER** — `backend/api/whatsapp_routes.py:1849-1868` (handler), `:130-136` (prompt text)
**Destructive `cset_restart_*` has no stage guard, no recency guard, no confirmation, and no progress-loss warning at the moment of the tap.**

Repro: at lesson 1, tap "🎬 Course language" → pick Hindi → tap "▶️ Continue here" (`cset_resume_hi`). Keep learning to lesson 14. Scroll up to that still-live two-button message and tap "🔁 From the start" (`cset_restart_hi`).
Traced: `reply_id.startswith("cset_")` at 1852 → `mode="restart"` → 1857 `lesson_index = 0`, 1858 `_reset_quiz_state(session)` (verified at 1247-1252: clears `quiz_index`, `quiz_correct`, `quiz_current`, `quiz_seen`), 1859 `assignment_draft = None`, commit, lesson 1 delivered. No `session.stage in SIGNUP_STAGES` check, no `is this message current` check — the exact class of bug the `lang_*` branch was already hardened against at 1762.
The only ⚠️ text is `CLANG_WARN` (:124), which is the body of the *picker list* and warns about language, not progress. The restart/resume buttons are a separate message whose body `CLANG_APPLY_PROMPT` (:132) reads only "Start from the beginning, or continue from your current lesson" — no mention of losing lessons.
Learner symptom: 14 lessons, all quiz history and the seen-question pool gone in one tap, irreversibly, with no confirm step.
Fix: guard `cset_restart_*` behind a fresh explicit confirmation ("this will erase your progress — you're on lesson N") and refuse it when `session.stage` is not the stage that issued the picker.

---

### 2. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1721-1729`
**`restart` / `start over` does not restart the course.** The only finding here that needs **no stale tap** — it hits every learner who types the word.

Traced: 1722-1726 write `language=None`, `stage="new"`, `opt_out=False`, `quiz_index=0`, `quiz_correct=0`. `lesson_index` is untouched (re-derived: the only `lesson_index =` writes in the file are 1358, 1741, 1857, 1862 — none reachable from this branch).
Repro: on lesson 12 type "start over" → picker → language → name → profile → goal → "✍️ Sign up" → "Confirm number" → walkthrough → "Let's start 🚀" → 1965-1966 `_send_lesson(..., session.lesson_index or 0)` → **lesson 12 again**.
Learner symptom: sat through the entire onboarding funnel a second time on an explicit promise of starting over, and landed back on the lesson they were trying to escape.
Fix: in the restart branch also set `lesson_index = 0`, `quiz_language = None` and call `_reset_quiz_state(session)`.

---

### 3. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1871-1877`
**`ask_name` has no `reply_id is None` guard — a stale button/row title becomes the learner's name.**

`_extract()` (857-870) returns the button/row **title** as `text` for every interactive reply, so `candidate = (text or "").strip()` at 1872 accepts it.
Repro (fresh learner): pick language → type name → profile list → goal list on screen. Scroll up, tap the original **language row** → 1762 (`stage="ask_goal" ∈ SIGNUP_STAGES`) → `stage="ask_name"`, name question re-sent. Now tap the stale **goal row** "🎯 Land an AI/tech job" → `reply_id="goal_job"` matches no branch above 1871 (verified: only `lang_`, `menu`, `quiz_lang`, `qlang_`, `course_lang`, `clang_`, `cset_` are handled at 1755-1868) → `session.name = "🎯 Land an AI/tech job"` → `_begin_onboarding`.
Learner symptom: every message, the pitch, the nudge templates and — via 1312-1319, `session.certificate_name` frozen at issue time — the printed certificate are addressed to a button label.
Fix: mirror `ask_profile`'s guard (`elif text and reply_id is None:` at 1892) — accept the name only when `reply_id is None`, else re-ask.

---

### 4. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1928, 1937-1940, 1944-1956, 1965-1969`
**`signup` / `confirm_number` / `howto_*` / `start_lesson` have no stage guard — a stale tap destroys a mid-course learner's stage.**

Repro: learner at lesson 9, `stage="assignment"`, draft typed. Tap the old "✍️ Sign up" button. Traced: the `assignment` block is at 2044+, so 1928 wins → number-confirm prompt. Tap "Confirm" → 1938 `stage="howto"` → walkthrough step 1. Tap "Skip Tutorial ⏭️" (1946) or "Let's start 🚀" (1966) → `stage="lesson"`.
Learner symptom: their assignment step is gone and `assignment_draft` is orphaned (nothing but stage `assignment` reads it); to reach that assignment again they must re-pass the quiz. If they instead go idle mid-walkthrough they sit in stage `howto`, which is a permanent nudge dead zone (finding 6).
Fix: gate all four on `session.stage in SIGNUP_STAGES | {"onboarded", "howto"}`.

---

### 5. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1812-1868` ordering vs `:1871`
**`course_lang` / `clang_*` / `cset_*` are matched before the onboarding handlers with no stage check, so a stale tap abandons signup mid-flow.**

Repro: finish signup, reach lesson 1. Type `restart` → picker → tap a language → 1763 `stage="ask_name"`, name question sent. Scroll up and tap the stale **"🎬 Course language"** → 1830-1832 sends the picker with no stage check → tap Hindi → tap "▶️ Continue here" → 1863 `stage="lesson"`, lesson delivered.
Learner symptom: name question never answered, no profile, no goal, no pitch, no `signup`/`confirm_number` step, no walkthrough — they are back in the course having skipped the entire sign-up funnel (and, because of finding 2, at their pre-restart lesson).
Fix: give the `course_lang` / `clang_` / `cset_` branches the same `SIGNUP_STAGES` early-out the `lang_` branch got at 1762.

---

### 6. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1912`
**`ask_goal` accepts a stale button's title as the goal — the sibling guard `ask_profile` already has.**

`goal = GOAL_MAP.get(reply_id) if reply_id in GOAL_MAP else (text or "").strip()`. Re-derived: `GOAL_MAP` keys are exactly `goal_job, goal_grow, goal_build, goal_explore` (923-928); `STATUS_MAP` keys are `prof_student, prof_graduate, prof_working, prof_jobseeker` (911-916).
Repro: brand-new learner → language → name → profile list → tap "🎓 Student" → goal list appears. Scroll up and tap the **profile** row "🎓 Student" again → `reply_id="prof_student"` not in `GOAL_MAP` → `goal = "🎓 Student"`, `stage="onboarded"`, committed.
Learner symptom: the goal question is silently marked answered and never re-asked; `session.goal` is a button label that feeds the funnel and the Teacher's context, and they are jumped to the free-offer/Sign-up screen.
Fix: `elif text and reply_id is None:` for the free-text branch (the question does invite free text), plus an "I didn't catch that" re-ask on an unrecognised `reply_id`.

---

### 7. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1504-1518` (`_resume_stage`) vs `:1521-1539` (`_rerender_quiz_step`)
**A language switch via `lang_*` or typed language leaves a stale `quiz_language`, so the next quiz question reverts to the old language.**

Only the `cset_` path clears the override (1855 `quiz_language = None`). The `lang_*` branch (1757) and the typed-language branch (1777-1778) set `session.language` alone. `_resume_stage` line 1509 renders the question with **`lang`**, not `_qlang(session)`; `_rerender_quiz_step` line 1527 correctly passes `qlang`.
Repro: tap "🌐 Quiz language" → Tamil (`quiz_language="ta"`). Tap "📝 Start quiz" → Q1 in Tamil. Type `hindi` → 1777 sets `language="hi"` → `_resume_stage` → Q1 re-sent in **Hindi**. Answer it → the turn's `qlang = session.quiz_language or lang` (1802) = **Tamil** → feedback and Q2 arrive in Tamil.
Learner symptom: they asked for Hindi, saw Hindi confirmed and Hindi on screen, and the very next question flips back to Tamil — permanently, for every quiz and assignment, with no visible control that undoes it.
Fix: clear `session.quiz_language` in the `lang_*` and typed-language branches (or have `_resume_stage` use `_qlang(session)` consistently — pick one, not both).

---

### 8. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:1514`
**`_resume_stage` calls `_send_assignment(frm, lang, assignment)` with no `skippable` argument** (defaults `False`), while `_rerender_quiz_step` computes it properly at 1533-1534 (`skippable = not _is_last_in_module(...)`).
Repro: on an *optional* assignment (not a module's last microlesson), type a language name → assignment re-sent with the "Skip for now" button gone.
Learner symptom: an optional assignment silently becomes compulsory-looking; a learner who can't do it has no way past it except guessing that Submit-with-junk works.
Fix: compute and pass `skippable` in `_resume_stage` exactly as `_rerender_quiz_step` does (and render it in `_qlang(session)`).

---

### 9. VERDICT CONFIRMED (narrowed) — SEVERITY **SHOULD-FIX** — `whatsapp_routes.py:2141-2154`
**Stages `onboarded` and `quiz_failed` swallow free text with no button re-offered.** (The `done` half of this claim is refuted — see below.)

Traced: stage `lesson` (2124-2137) answers via the Teacher **and** re-sends `quiz`/`quiz_lang`/`course_lang`. Nothing matches for `onboarded` or `quiz_failed`, so both fall to 2153-2154 `_teacher_answer` alone.
Repro A (`onboarded`, the conversion point): answer the goal question → "saved" + ₹699-free offer + a single "✍️ Sign up" button (1921-1924). Type "how long does it take?" → Teacher answer, nothing else. Verified there is no typed route to sign-up: `signup` exists only as a `reply_id` (1928); `low` command matching covers unsubscribe/refer/restart/menu only. Every further message produces more Teacher chat while the CTA is buried, and typing keeps bumping `last_active_at`, so the `start_lesson` nudge (2 h idle) never fires either.
Repro B (`quiz_failed`): fail the quiz → "Retake" button (2038-2040). Type "why was that wrong?" → Teacher answer, no Retake. `retake` is likewise reply_id-only (1990).
Learner symptom: the single way forward scrolls off screen and no typed word brings it back.
Fix: re-offer the stage's own button after `_teacher_answer` for `onboarded` (Sign up) and `quiz_failed` (Retake), as stage `lesson` already does.

---

### 10. VERDICT CONFIRMED — SEVERITY **SHOULD-FIX** — `whatsapp_drip.py:55, 67-77`
**Stage `howto` has no nudge rule — the deepest point of the funnel is never re-engaged.**
Re-derived by importing the module: `SIGNUP_STAGES ∪ all NUDGE_RULES stages` covers `ask_goal, ask_name, ask_profile, assignment, between_lessons, clarify, done, lesson, new, onboarded, quiz, quiz_failed, welcome`. Uncovered stages actually written in `whatsapp_routes.py`: **`howto`** (set at 1938) and **`practice`** (set at 1545).
`howto` is reached only *after* "✍️ Sign up" and "Confirm number". `HOWTO_STEPS` is a 5-message walkthrough (946-989); anyone who stops tapping partway reaches `_pick_nudge` (176), matches nothing at 196-198, and returns `None` forever. Finding 4 makes `howto` reachable from mid-course too. Same shape as the `between_lessons`/`clarify` gap already fixed.
Fix: add `("finish_signup"|"start_lesson", {"howto", "practice"}, 3)` to `NUDGE_RULES`.

---

### 11. VERDICT CONFIRMED — SEVERITY **NITPICK** — `whatsapp_routes.py:1958-1962`
Typing anything mid-walkthrough calls `_send_howto_step(frm, lang, 0)` unconditionally — a learner on step 4 who asks a question is thrown back to step 1. Fix: persist the walkthrough index on the session and re-show the current step.

---

## REFUTED (4 sub-claims)

1. **Finding 3's `qlang_*` variant** ("re-offered Start quiz → line 1990 → `stage="quiz"` for a learner who never signed up") — **wrong code path.** `if session.stage == "ask_name"` at 1871 sits *above* line 1990, so tapping "📝 Start quiz" at stage `ask_name` never reaches `_start_quiz`; it is consumed at 1872 and becomes `session.name = "📝 Start quiz"`. That is finding 3's (my #3) defect, not a separate quiz-without-signup path. The `clang_`/`cset_` half of the claim stands (my #5).
2. **Finding 4's "`quiz_current` survives, so a stale question set can be re-served"** — **refuted.** `_select_quiz` (1122-1135) unconditionally overwrites `session.quiz_current` on every quiz start, and after a restart `stage="new"`, so no path reads the old value before it is replaced. Only `lesson_index` is the real leak.
3. **Finding 4's list of surviving fields as harms** (`current_status`, `goal`, `assignment_draft`) — overstated: re-onboarding overwrites `current_status` (1899) and `goal` (1917), and an orphaned `assignment_draft` is only read at stage `assignment`. `quiz_language` surviving is real but minor.
4. **Finding 9's `done` component** — refuted. Stage `done` is not a bare fall-through: 2141-2151 delivers the certificate (returning if it fired) or auto-advances when more lessons exist. Once the course is genuinely finished there is no forward button to re-offer, so Teacher-only is correct there.

Also not filed as defects (agreeing with the sweep's own classification): the English-only `_send_language_picker` (874-883) is cosmetic; the `restart`-then-`JOIN` referral misattribution is capped by `core/referrals.py:70-75` to misattribution, not payout abuse; and the pre-commit windows at 1017-1027 / 1899-1907 are a real but unobserved race, correctly left as a note.